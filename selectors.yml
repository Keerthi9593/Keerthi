selectors:
  - name: silver_models
    description: Run all silver layer models
    definition:
      method: tag
      value: silver

  - name: gold_models
    description: Run all gold layer models
    definition:
       method: tag
       value: gold